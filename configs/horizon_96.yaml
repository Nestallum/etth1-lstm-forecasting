# ETTh1 LSTM Forecasting — horizon 96 configuration

data:
  csv_path: data/raw/ETTh1.csv
  lookback: 192
  horizon: 96
  target_index: 0
  num_workers: 0

training:
  batch_size: 64
  epochs: 30
  learning_rate: 0.001
  weight_decay: 0.0
  early_stopping_patience: 5
  grad_clip: 1.0

model:
  input_size: 1
  hidden_size: 128
  num_layers: 2
  dropout: 0.2
  revin: true
  
paths:
  checkpoints_dir: checkpoints/h96
  logs_dir: logs/h96
  results_dir: results/h96

seed: 42

device: auto  # auto | cpu | cuda